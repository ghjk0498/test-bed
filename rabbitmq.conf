# rabbitmq.conf

# guest 계정의 외부(원격) 접속을 허용 (기본값은 localhost만 허용)
loopback_users.guest = false

# AMQP 통신을 위한 기본 포트 설정 (기본값: 5672)
listeners.tcp.default = 5672

# 웹 관리 UI 접속을 위한 포트 설정 (기본값: 15672)
management.tcp.port = 15672

# 클러스터 구성 방식 설정: 설정 파일에 명시된 노드 목록을 사용하는 classic_config 방식 사용
cluster_formation.peer_discovery_backend = classic_config

# 클러스터에 참여할 노드 정의 (rabbit@hostname 형식)
cluster_formation.classic_config.nodes.1 = rabbit@rabbit1
cluster_formation.classic_config.nodes.2 = rabbit@rabbit2
cluster_formation.classic_config.nodes.3 = rabbit@rabbit3
